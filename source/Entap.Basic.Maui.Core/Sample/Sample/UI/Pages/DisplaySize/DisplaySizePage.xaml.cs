namespace Sample;

public partial class DisplaySizePage : ContentPage
{
	public DisplaySizePage()
	{
		InitializeComponent();
	}

    //protected override void OnSizeAllocated(double width, double height)
    //{
    //    base.OnSizeAllocated(width, height);
    //    System.Diagnostics.Debug.WriteLine($"{nameof(OnSizeAllocated)} width : {width}  height:{height}");
    //}
}
